# LLM API Keys (Replace with your actual keys)
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY_HERE"
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Optional: Default models and API configurations
# Set a default model for the initial response (R1). 
# If unset, the backend might default to the first available model found.
# Example: DEFAULT_LLM_MODEL_ENV="gpt-4o"

# Set a default model for the ethical analysis response (R2).
# This is REQUIRED if not provided dynamically by the frontend.
ANALYSIS_LLM_MODEL_ENV="YOUR_DEFAULT_ANALYSIS_MODEL_HERE" # e.g., "claude-3-sonnet-20240229"

# Set the Anthropic API version if needed (defaults to 2023-06-01)
# ANTHROPIC_API_VERSION="2023-06-01"

# Optional: Custom API Endpoints (Uncomment and set if using proxies, Azure OpenAI, etc.)
# GEMINI_API_ENDPOINT="YOUR_GEMINI_ENDPOINT_HERE"
# ANTHROPIC_API_ENDPOINT="YOUR_ANTHROPIC_ENDPOINT_HERE"
# OPENAI_API_ENDPOINT="YOUR_OPENAI_ENDPOINT_HERE" 